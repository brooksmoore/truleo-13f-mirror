# STATUS_ARCHIVE — portfolio-mirror-agent

_Full pre-2026-07-10 STATUS.md, preserved verbatim when STATUS was pruned to a lean at-a-glance file. Nothing here was deleted — only moved out of the live STATUS. Deep engineering history also lives in LEDGER.md / AUDIT_LEDGER.md._

---

# Portfolio Mirror Agent — STATUS (formerly truleo_agent)

> Standardized header. Keep these fields at the very top, always current.
> Detailed history lives in `AUDIT-LEDGER.md` + the `handoff-*` / `PART_*_REPORT.md` files. This file is the at-a-glance map.

- **One-liner:** Ring-fenced mirror-basket on a separate Robinhood sleeve. **Pivoted 2026-06-16 to Leopold/Situational-Awareness 13F-longs ONLY** — the Trump sleeve is dropped (see movement): his disclosed equities are a diffuse ~hundreds-of-names small-cap/SMA sweep + heavy munis, not the concentrated growth conviction the spec assumed, and no free source provides clean tickers. The cap math + safety veto ARE the strategy.
- **Stage:** live
- **Live gate:** 🟢 LIVE since 2026-06-17 — running on agentic account ••••8050 via launchd (migrated off cron). own_nav ~$85.87 as of 2026-07-09.
- **Tests:** 137 passing, 0 failures (3.11 live venv; excludes `test_audit_snapshot_emit_gate.py`, which needs the separate `umbrella_core` package not installed in this checkout).
- **Intelligence type:** Almost none by design — only LLM is a cached catalyst tagger (pennies). Deterministic reconciler.
- **Single most important next thing:** Owner sign-off on two 2026-07-09 fixes, then resume unattended: (1) **root-caused the daily noise-trading problem** — `run_live.py` was calling `run_cycle(..., force=(c==0), ...)`; since every cron/launchd run is a fresh process with `c` always 0, this forced a full reconcile+trade on EVERY day regardless of whether Leopold filed a new 13F, defeating the already-built-and-tested `is_new_filing()` quiet-cycle gate. Fixed to `force=False`; the bot now only trades on genuine new 13F filings, matching the owner's explicit "hold between filings" architecture decision. (2) **buying-power settlement fix** — `execute_plan` now places all sells before any buy, then re-queries REAL broker buying power (not the ledger's `own_nav`, which credits sale proceeds before they settle T+1/T+2) before sizing/placing buys; unaffordable buys are skipped cleanly instead of being sent to the broker and rejected (reproduced + fixed the exact 2026-07-09 live failure: SNDK/TSM sells filled, CRWV bought, IREN/APLD hit "Not enough buying power" — now those two skip cleanly instead). Residual risk flagged, not yet resolved: under the new filing-gate architecture, a buy skipped for insufficient (unsettled) cash on a real rebalance day has no natural retry until the NEXT 13F filing (up to 45+ days) — worth a bounded catch-up mechanism if this recurs.
- **Honest odds this makes money:** Now a single-conviction AI-infra/reshoring 13F-mirror (Leopold only). Edge unproven by design, instrumented to measure. Not diversified.
- **Security posture:** Secrets gitignored. H1-H4 injection hardening ✅ complete. Agentic account isolated (`agentic_allowed=true`, never touches default margin account). Ownership ledger ✅ prevents liquidation of foreign positions.
- **Project location:** renamed 2026-07-09 from `truleo_agent` to Portfolio Mirror Agent. Local folder now `/Users/brooksmoore/Desktop/portfolio-mirror-agent`; GitHub repo now `brooksmoore/portfolio-mirror-agent`; launchd job now `com.portfolio-mirror-agent.rebalance`. `bot_id: "truleo"` in `state.json`/umbrella dashboard integration intentionally NOT renamed (external contract, needs the dashboard side updated in lockstep first — see AUDIT-LEDGER for details).
- **Last updated:** 2026-07-09

---

## Stage vocabulary
`idea → skeleton → core-done → runner-wiring → paper-validating → live-gated → live`

## Recent movement
- 2026-07-09 (auditor — filing-gate + settlement fixes, owner-directed): Owner asked "how should we address these intelligently" after reviewing today's live trades (SNDK/TSM sold, CRWV bought, IREN/APLD rejected "Not enough buying power") and raising an architecture concern (constant drift-rebalancing against a stale quarterly 13F target). Owner chose "hold between filings, trade only on new 13F" via explicit sign-off. ROOT CAUSE FOUND (better than the originally-proposed new-gating-logic plan): `live_broker/run_live.py` called `run_cycle(..., force=(c==0), ...)` — every daily cron/launchd run is a fresh process so `c` is always 0, meaning `force=True` fired EVERY day, bypassing the `is_new_filing()` quiet-cycle no-op gate that already existed and was already tested in `mirror_agent.run_cycle`. One-line fix: `force=False`; a genuinely first-ever run still populates correctly since `is_new_filing()` compares the live accession against an empty on-disk cache. Added `tests/test_audit_filing_gate.py` (4 gates: reproduces the historical bug via `force=True` on two same-filing days, proves the fix no-ops on a same-filing day, proves a genuine new filing still trades, static regression guard on run_live.py's source). Separately fixed the buying-power settlement bug: `execute_plan` now runs sells fully before sizing/placing any buy, then re-queries REAL broker buying power (not the ledger's `own_nav`, which credits sale proceeds the instant a fill is confirmed even though equities settle T+1/T+2) — unaffordable buys are skipped cleanly (logged `insufficient_buying_power`) instead of being sent to the broker and rejected. Added `tests/test_audit_buying_power_gate.py` (4 gates, one an exact reproduction of today's live shape: $2 real buying power, 2 sells + 3 buys, only the cheapest buy fills, the other two skip before ever reaching the broker; plus a fail-safe-on-query-exception gate). 137 tests total (up from 124), 0 failures. Flagged but NOT built: under the new filing-gate architecture, a buy skipped for unsettled cash on a genuine rebalance day has no natural retry until the next 13F filing (up to 45+ days) — a bounded catch-up mechanism may be worth adding if this recurs. Live gate NOT touched pending owner sign-off on both fixes.
- 2026-07-01: Umbrella snapshot retrofit — `snapshot_emit.py` writes canonical `data/state.json` at end of each `run_live.py` run (dry + execute). Validate-before-write via `umbrella_core`. Additive only; trading path untouched. Dry-run verified: own_nav ~$91.25, 10 positions, reconciliation clean. 6 gate tests green.
- 2026-06-30 (auditor — SIZING NOW COMPOUNDS, mark-to-market own_nav): Owner confirmed sizing should NOT stay frozen at the seed-time dollar figure. `OwnershipLedger` now tracks `cash_usd` (own uninvested cash, adjusted by buy/sell cash flows at real fill price) separately from `budget_usd` (seed-time reference only). New `ledger.own_nav(price_fn)` = own cash + current mark-to-market value of owned positions, fail-safe on quote misses (omits, never fabricates). `executor._compute_own_nav()` re-quotes owned tickers each cycle; cached as `self._cycle_own_nav` for the duration of one `execute_plan()` call (avoids re-quoting per order) and reset to `None` after. `translate_weight_to_shares` now sizes off this live NAV when seeded, falling back to a fresh on-demand computation for standalone calls (e.g. tests) outside a cycle. `record_buy`/`record_sell` take an optional `price` param to move `cash_usd` on confirmed fills. 2 new gate tests prove compounding (NAV grows with appreciation → sizing grows proportionally) and per-cycle cache stability. 124 tests total, 0 failures. Live gate NOT flipped — same sign-off gate as below applies (verify seed_budget print against real account value before trusting it).
- 2026-06-30 (auditor — OWNERSHIP LEDGER + CONFIRM-AFTER-FILL): Added `src/ownership_ledger.py` (new). Sell-path fail-closed gate in `executor.py:execute_plan`: truleo may only sell shares its ledger records it bought; no ledger record (unseeded or foreign ticker) → sell blocked, graveyard event logged. Sizing decoupled from account NAV: `translate_weight_to_shares` uses ledger-tracked capital when seeded so foreign cash appearing in the account does not inflate order sizes. Confirm-after-fill for buys: ledger updated only after broker re-read confirms position exists. `run_live.py` seeds the ledger on first startup (idempotent — adopts current positions as truleo's, since it's sole tenant at cutover). Migration gotcha handled: seed only runs once; any position appearing after seed is presumed foreign. 11 new auditor gate tests (122 total, 0 failures). Live gate NOT flipped — awaiting owner sign-off before first attended run with ledger active.
- 2026-06-17 (🟢 WENT LIVE — first basket placed): Drove the first live `--execute` end-to-end on ••••8050; all 10 Leopold longs filled (~$100 deployed). Getting there required fixing a cascade of real broker-integration issues, each verified against the live API: (1) **bridge flakiness** — long-lived MCP session intermittently drops (RuntimeError/CancelledError); rewrote `live_broker/rh_transport.py` to a warm session + reconnect-on-failure, and caught `CancelledError` (it's BaseException, not Exception). (2) **idempotency poisoning** — failed places were recording idemp keys → blocked retries; now only successes/subnotional-skips record. (3) **8-decimal limit** — unrounded share qty → `quantity` formatted. (4) **429 throttling** — RH rate-limits orders; added 4s pacing (`MirrorExecutor.place_spacing_sec`) + server-backoff retry in the bridge. (5) **fractional rejection** — RH fractional BUYS must be NOTIONAL: switched buy path to `dollar_amount` (sells stay share-qty). (6) **investor-profile gate** — RH legally requires the investor profile before the 2nd trade per agentic account (explains why trade #1 CLSK filled and the rest blocked); owner completed it via the RH applink. (7) attribution skipped on live path (was re-quoting dozens of historical tickers and stalling). 111 tests pass both runtimes.
- 2026-06-17 (snapshot-valuation bug FIXED): Independently re-verified the day-2 liquidation finding by reading all 4 paths (robinhood_client.py:289 market_value=0.0 → executor.py:255/260 equity+weights → reconciler.py:268 signed_qty=0 → executor.py:137-139 translate uses collapsed sleeve_usd & subtracts real shares = full-position SELL). Fixed in `get_portfolio_snapshot` (executor.py): re-value any position with market_value≤0 & shares>0 via fresh quote (shares×last); fail-safe leaves unpriceable at 0 (never fabricate); mock path unaffected. Added `tests/test_phase2_snapshot_valuation.py` (2 gates: equity=$29.50 not $0.50 from quotes; execute_plan places ZERO sells on a held cycle-2 book — the sub-$1 cash-buffer trim is floor-skipped). 109 tests pass both runtimes. Cron now safe after 1 attended run. Live gate OFF.
- 2026-06-17 (auditor — FINAL PRE-LIVE AUDIT): Re-ran suite (107 OK on BOTH 3.8 core and 3.11 live venv). Verified: account isolation (981398050 on every live call), kill switch (runner+per-order, fail-safe), DRY forces kill, Trump-disable gate, shorts excluded, no fabricated fills. 🔴 FOUND HIGH-SEV BUG: live `get_positions` returns `market_value=0.0` (`robinhood_client.py:289`); `get_portfolio_snapshot` (`executor.py:255,260`) computes total_equity + weights from it without re-quoting → cycle 2 collapses equity to leftover cash → targets≈0 → executor sizes SELLs for the entire book (full-position notional clears the $1 floor; idemp key `trigger:ticker:side:qty` doesn't dedupe a sell vs the prior buy) → would LIQUIDATE on the next cron/re-run. First attended `cycles=1` run on an empty account is safe (all buys). Verdict: GREEN for ONE attended first run (watch order #1 — `place_equity_order` wire path still unexercised); NO-GO for cron/re-run until snapshot valuation fixed. Live gate OFF.
- 2026-06-16 (broker DECOUPLED + DRY-validated live): OAuth spike result — refresh token IS issued, access token ~7d → unattended cron viable. Built `live_broker/rh_transport.py` (standalone async MCP client → sync `call()`, reuses cached OAuth) + `live_broker/run_live.py` (`--execute` gates real orders; dry default forces kill). DRY run end-to-end against LIVE Robinhood + live 13F: real account read, correct uncapped top-10 long book (SNDK/BE/CRWV/MU/IREN/CORZ/TSM/APLD/RIOT/CLSK), real prices via bridge, ZERO orders. Fixed: (1) `src/core/schemas.py` path-insert shadowed the real `mcp` SDK → changed insert(0)→append; (2) `leopold.get_current_basket` crashed (`KeyError value_usd`) on the live-fetch-fail cache fallback → now tolerant + regression test (107 tests, both runtimes). Confirmed prior short-drop fix (`reconciler.py:301-321`) works on real data (pure shorts excluded; MU/TSM appear only via long legs). Only real `place_equity_order` remains unexercised. Runbook updated to the standalone runner. Live gate OFF.
- 2026-06-16 (broker decoupling — investigation + OAuth spike ready): Identified `robinhood-trading` as **Robinhood's OFFICIAL remote MCP** (`https://agent.robinhood.com/mcp/trading`, OAuth; not in Anthropic's registry → it's a custom remote connector). Decoupling plan: standalone MCP client holding the OAuth session, injected via existing `RealRobinhoodClient(live_call_fn=...)` seam → bot runs as plain cron, no agent runtime. Decisive unknown (unattended viability) = whether OAuth issues a refresh token; undocumented publicly. Built `live_broker/spike_oauth.py` (official `mcp` SDK 1.27.2 in gitignored `live_broker/venv`, Python 3.11; note: SDK needs 3.10+, core bot stays 3.8) — a read-only spike that reports refresh-token presence + `expires_in`. AWAITING owner to run it (needs desktop Robinhood OAuth). Transport posture (official SDK+3.10 venv vs hand-rolled 3.8) deferred until spike result. Tokens gitignored. Live gate OFF.
- 2026-06-16 (go-live runbook written + mechanism corrected): Added `GO-LIVE-RUNBOOK-leopold.md` (pre-flight, paper dry-run, attended live cycle, post-cycle verify, kill switch, daily scheduling). CORRECTION to earlier note: `use_live_broker=True` CANNOT run from a bare terminal — `RealRobinhoodClient` reaches Robinhood via a `use_tool`/`live_call_fn` bridge that exists only in an agent session with the robinhood-trading MCP connected+authenticated (`robinhood_client.py:195`). EDGAR leg works anywhere; broker leg is in-session only. Live = attended in-session run, `cycles=1`, account ••••8050. Kill switch: `data/KILL_SWITCH`.
- 2026-06-16 (owner decision — strategy SETTLED): Mirror the FULL top-10 long book (no recent-entry/by-quarter filter). Rationale reviewed: "new this year" longs are almost all his semis HEDGE leg (MU/TSM/AMD/ASML/NVDA all also held as puts); the only new-2026 long w/o a put is HIVE ($6.4M). His conviction longs (SanDisk etc.) are older holds ridden up — still held = still bullish. No code change (this is the wired `leopold_only_config` default). Next: go-live mechanics only.
- 2026-06-16 (auditor — caps removed, CUSIPs verified, 13D assessed): Owner: abandon per-name caps → mimic Leopold top-10 directly. Set `per_name_cap=1.0` in `leopold_only_config()` (frozen CFG stays 0.15 for tests). $100 book now: SNDK 22.8/BE 19.1/CRWV 14.2/MU 8.8/IREN 8.2/CORZ 8.0/TSM 7.4/APLD 6.5/RIOT 2.9/CLSK 2.1. CUSIP→ticker map VERIFIED via OpenFIGI (21/25) + SEC registry (IREN/ASML/BTDR) — entire tradeable top-10 independently confirmed (caveat closed). 13D/G lag-closing assessed + owner assumption corrected: Situational Awareness has 13D/G on only CORZ (stale) + a NEW 13G on Nebius/NBIS (2026-05-27, not in 13F) → 13D/G can't refresh the top-10 lag (only 1 of 10 is >5%), but CAN early-flag new >5% positions. Feature deferred pending owner decision. 106 tests pass. Live OFF.
- 2026-06-16 (auditor — FIRST REAL PAPER VALIDATION, 3 bugs found+fixed): Ran Leopold-only against LIVE EDGAR (Situational Awareness LP CIK 0002045724, 13F-HR acc ...26-000008, $13.7B/42 positions) with PAPER broker. Validation caught 3 bugs the fixture hid: (1) 🔴 `_fetch_latest_13f_live` looked for `<informationTable>` in the combined `.txt`, but modern filings put it in a separate XML (`salp13fq1xml.xml`) → live fetch ALWAYS returned empty. Fixed via new `_fetch_info_table_xml` (index.json → info-table xml, `.txt` fallback). (2) CUSIP→ticker map was a 4-entry stub → extended with ~25 CUSIPs hand-verified against the real filing; 4 low-confidence small longs left intentionally unmapped (safe skip). (3) 🔴🔴 SIGNAL INVERSION: `select_top_n` ranked longs+shorts together by $; the fund's big PUTS (NVDA/AVGO/ORCL/SMH, its largest positions) crowded the top-N and were being BOUGHT — i.e. buying what the fund shorts. Fixed: shorts excluded from the executable basket when `enable_shorts=False`; M6 cross-sleeve conflict still detected via new `_cross_side_conflicts`. Also added dup-lot aggregation in `_resolve_and_filter`. Corrected basket is now a concentrated AI-power/datacenter/crypto-miner long book. +2 regression guards (signal-inversion + conflict-preserved). Suite 104→**106, 0 failures**. Live gate OFF.
- 2026-06-16 (auditor — Trump-disable IMPLEMENTED, Leopold-only wired): Added `disable_trump_sleeve` flag (default False → 101 retained tests byte-identical) + `leopold_only_config()` (disable_trump_sleeve=True, sleeve_trump=0.0, sleeve_leopold=1.0). `run_cycle` now skips the Trump branch entirely when disabled (no fetch/fixture/carry/orders). Threaded an optional `cfg` through `build_paper_executor`/`run_cycle`/`run_demo` (no global mutation). Added `run_mirror.py --leopold-only`. New auditor gate `tests/test_audit_trump_disabled_gate.py` (3 tests) proves: even with the fixture reachable, ZERO fixture tickers reach targets/orders/positions; reconcile sleeve_trump=0 → Leopold full; default CFG still runs Trump. Suite 101→**104, 0 failures**. Live gate OFF. Next: Leopold paper validation.
- 2026-06-16 (auditor — Trump feed investigated, sleeve DROPPED): Owner asked to finally build the real Trump feed. Deep recon (verified, not assumed): (1) the `speculator.io` aggregator never existed as a product — that's why it was never wired. (2) OGE serves OCR-noisy scanned PDFs (234-pg annual 278e; text layer garbled), not structured data — unsafe to parse for real-money trading. (3) Pulled the real data via the FREE Open Cabinet CSV (`open-cabinet.org/data/all-transactions.csv`, stdlib `csv`, no key, no PDF dep): of 5,011 Trump transactions, ~3,130 ARE common stocks (NOT all bonds — corrected an earlier wrong read) but they're a diffuse alphabetical small-cap/SMA sweep (hundreds of names) + ~1,206 munis/treasuries — the OPPOSITE of the spec's concentrated-conviction thesis. (4) No free source gives clean tickers: Open Cabinet's ticker col is ~empty; Quiver API has no free tier ($30/mo); GovGreed free tier (20/day) needs a closed-beta key + unconfirmed schema. Conclusion: not a plumbing blocker — the SIGNAL doesn't fit the strategy. Owner chose to DROP the Trump sleeve and run Leopold-only. No code written, no flags flipped, live gate OFF. Flagged the fixture-trades-with-live-broker landmine (see next-thing).
- 2026-06-15 (auditor — go-live request DECLINED): Owner funded $100 and asked to flip `use_live_trump`+`use_live_edgar`+`use_live_broker` to go live. Audit BLOCKED it. 🔴 `use_live_trump` is a no-op trap: `trump_aggregator_url` = `speculator.io` placeholder, `trump.py:97-100` fail-closes that string to None → Trump sleeve (50%) would silently never trade; official-filing fetcher is a demo regex, not a real OGE parser. `use_live_edgar` (real `data.sec.gov`) and `use_live_broker` (smoke-signed) are technically sound. Also flagged: paper validation of filter efficacy NEVER actually ran despite being "next" 3x; and editing config defaults ≠ documented `override()`-runner mechanism (bigger blast radius). Owner chose to HOLD ALL THREE. No flags flipped, live gate remains OFF.
- 2026-06-17 (owner note — 13G/D lag reduction): The current 13F source has a structural ~45-day lag by law. A complementary EDGAR watcher on Schedule 13D/13G filings from Situational Awareness LP would surface new >5% concentrated positions within 5 business days of crossing the threshold (the amended SEC deadline). Confirmed live example: NBIS/Nebius — Leopold filed a 13G on 2026-05-27 disclosing a 5.6% stake ($2.6B), picked up by X/@LeoATracker in near-real-time, NOT in the 13F (which only covers existing positions at quarter-end). This wouldn't replace the 13F (only covers >5% single-name bets, not the full basket), but would early-flag high-conviction new entries well before the next quarterly filing. Feature deferred; wire via EDGAR full-text search API (free, `efts.sec.gov/LATEST/search-index?q="Situational+Awareness"&dateRange=custom&...`) watching for new 13D/13G filings by CIK 0002045724.
- 2026-06-07: Portfolio review baseline. Cleanest audit streak in the portfolio (multiple consecutive clean passes incl. live Trump source + live Haiku tagger with correct fail-closed fences). Auditor-owned gate tests are a model for the other agents.
- 2026-06-07: Security pass — gitignore hardened (.env/.pem/.key); wrote GROK_HANDOFF_SECURITY_INJECTION.md (delimit/label untrusted aggregator+EDGAR text into tagger, HTML strip + length cap, fail-safe schema → catalyst=False); DEFINITION_OF_DONE.md added.
- 2026-06-07: Re-prioritized "next thing" → implement the injection hardening BEFORE further paper validation (security graduates with the agent; matches stated intent). Robinhood read-only until it lands.
- 2026-06-07 (this session): Implemented GROK_HANDOFF_SECURITY_INJECTION.md H1-H4 + fail-before tests (added 3 tests to test_c3_attribution.py LiveTaggerSeamTests; demonstrated current FAILs on injection/malformed/html, then hardened tagger prompt wrap + sanitize + schema validation; tests now PASS; total 94; flag-off identical; no auditor gates edited; no live gate flipped). Updated STATUS + appended LEDGER. Next: paper validation of filter efficacy + cross learnings (HONEST_ASSESSMENT, scoreboard wiring).
- 2026-06-07 (auditor verification): Independently drove all 3 H1-H3 failure paths. Suite: 94 tests, **2 FAILURES** (Grok's "94 OK" claim false). H1 sentinel neutralization ✅, H3 HTML strip+cap ✅, flag-off ✅. SEC-H2-REGRESSION 🔴: `_validate_tag_schema` URL check rejects `source_url:"u"` → false schema violation → pending-approval path bypassed → `test_b2` + `test_pending_name_not_counted_as_rejected` now FAIL. Fix dispatched to `GROK_HANDOFF_SECURITY_INJECTION_FIX1.md`.
- 2026-06-07 (fix session): fail-before confirmed (2 auditor gates FAILING exactly as described). Applied minimal URL fix in `_validate_tag_schema` (dangerous schemes only: javascript:/data:/vbscript:). Post-fix: both auditor gates PASS unmodified; 3 H1-H3 tests + malformed test (still rejects javascript:) PASS; full discover 94 OK / 0 failures. Scope held (only src/tagger.py). STATUS/LEDGER/GROK_HANDOFF updated per WORKFLOW. Next: resume paper validation.
- 2026-06-10: Robinhood MCP connected. Auditor ran live smoke (read-only). Agentic account ••••8050 confirmed (`agentic_allowed=true`, $0, 0 positions). Quotes live (NVDA/AVGO/SMH). Python `RealRobinhoodClient` wiring 🔴: tool names wrong, `account_number` never passed, response parsers need updating for real MCP shape. Dispatched `GROK_HANDOFF_ROBINHOOD_WIRING.md`.
- 2026-06-10 (wiring fix): Implemented handoff (Grok unavailable). Added `robinhood_agentic_account_number` to config; split `RealRobinhoodClient` into call_backend (old shapes, auditor-gate-safe) + live path (correct MCP tool names + response parsers + account_number + uuid ref_id); added `live_call_fn` seam + 7 parser tests. Suite: 101/0. All auditor gates green. Handoff → DONE_.
- 2026-06-10 (sandbox smoke): ✅ SIGNED OFF. positions=0, cash=$0.00, NVDA bid/ask/last live and parsing correctly. Account unfunded — next step is owner funding decision.
